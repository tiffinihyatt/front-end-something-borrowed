//
//  BrowseView.swift
//  something-borrowed
//
//  Created by Tiffini Hyatt on 8/2/22.
//

import SwiftUI

struct BrowseView: View {
//    create new property to hold list of garments from API request
//    change List View to convert each garment into a specific UI
    
    var body: some View {
        List {
            Text("Garment")
            Text("Garment")
            Text("Garment")
        }
    }
}

struct BrowseView_Previews: PreviewProvider {
    static var previews: some View {
        BrowseView()
    }
}
